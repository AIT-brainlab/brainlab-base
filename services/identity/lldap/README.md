# `lldap` User Directory Service

## Overview
`lldap` manages Unix `posixAccount` UIDs/GIDs for lab members, ensuring NFS NAS file permissions on `cairo:/mnt/HDD/home` and Linux SSH logins match across all nodes.

## Quickstart
```bash
docker compose up -d
```
Admin interface: `http://<HOST_IP>:17170`
